<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.solutions.medadhere.medadheresolutionsapp.MedicationActivity"
    tools:showIn="@layout/app_bar_medication"
    android:weightSum="1">

    <Button
        android:paddingTop="@dimen/activity_vertical_margin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="NFC Available"
        android:layout_gravity="center_horizontal"
        android:visibility="invisible"
        android:id="@+id/btnTag"
        tools:foregroundGravity="bottom|center_horizontal"
        android:gravity="bottom|center_vertical|center_horizontal" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">


        <CalendarView
            android:id="@+id/calendarMed"
            android:layout_margin="5dp"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:weekDayTextAppearance="@android:style/TextAppearance.Medium"
            android:dateTextAppearance="@android:style/TextAppearance.Medium"
            android:showWeekNumber="false"/>

        <TextView
            android:id="@+id/instructions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical|center_horizontal"
            android:text="Click on a date from the calendar above to record a dose taken. " />

        <Button
            android:id="@+id/currDateButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Log for current Date"
            android:layout_gravity="center"/>

    </LinearLayout>
</LinearLayout>